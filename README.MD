Containerized Apache Web Server running on container port 80, mapped to host port 8089.

To Start:

Navigate to the base directory which contains docker-compose.yml, and run 'docker-compose up --build'